import React from "react";

const SendOTP: React.FC = () => {
  return (
    <React.Fragment>
      <p>
        SendOTP module is currently under development. Stay tuned for updates!
        🚧🚩
      </p>
    </React.Fragment>
  );
};

export default SendOTP;
